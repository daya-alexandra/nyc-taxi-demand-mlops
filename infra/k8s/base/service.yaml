apiVersion: v1
kind: Service
metadata:
  name: nyc-taxi-demand-api
  namespace: nyc-taxi-demand
  labels:
    app: nyc-taxi-demand-api
spec:
  type: NodePort
  selector:
    app: nyc-taxi-demand-api
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
